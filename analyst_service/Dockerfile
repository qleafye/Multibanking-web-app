# Используем официальный образ Python 3.10 в "slim" версии (меньше по размеру)
FROM python:3.10-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл с зависимостями в рабочую директорию
COPY requirements.txt .
# Устанавливаем зависимости, указанные в requirements.txt
RUN pip install --no-cache-dir --upgrade -r requirements.txt

# Копируем все остальные файлы проекта (main.py, logic.py и т.д.) в рабочую директорию
COPY . .

# Указываем команду, которая будет выполняться при запуске контейнера
# Запускаем uvicorn сервер, который будет обслуживать наше FastAPI приложение (main:app)
# --host 0.0.0.0 делает сервер доступным извне контейнера
# --port 8000 указывает порт, на котором будет работать сервер
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
